[build]
builder = "RAILPACK"
buildCommand = "pnpm -F '@swiftie-api/server...' build"

[deploy]
startCommand = "node packages/api/dist/src/main.js"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
